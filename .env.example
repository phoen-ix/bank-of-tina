# Flask Configuration
# Generate a strong random key, e.g.: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key

# ──────────────────────────────────────────────────────────────────────────────
# SMTP credentials are now configured inside the web UI under Settings → Email.
# The variables below are only needed if you want to use the standalone
# send_weekly_email.py script directly (outside of Docker / the web app).
# ──────────────────────────────────────────────────────────────────────────────

# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# FROM_EMAIL=your-email@gmail.com
# FROM_NAME=Bank of Tina
